import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    borderWidth: 2,
    borderColor: '#000',
    borderRadius: 4,
    marginTop: 20,
    padding: 15,
    display: 'flex',
    flexDirection: 'column',
  },
  cardDetails: {
    fontSize: 18,
  }
});